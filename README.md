# 🔐 Система Управління Користувачами

Повнофункціональна веб-система для управління користувачами з рольовим доступом, побудована на **React** + **Express**.

## 📋 Зміст

- [Огляд проєкту](#-огляд-проєкту)
- [Функціональні можливості](#-функціональні-можливості)
- [Технології](#-технології)
- [Структура проєкту](#-структура-проєкту)
- [Встановлення та запуск](#-встановлення-та-запуск)
- [Ролі користувачів](#-ролі-користувачів)
- [API Endpoints](#-api-endpoints)
- [Тестові облікові дані](#-тестові-облікові-дані)

---

## 🎯 Огляд проєкту

Це навчальний проєкт системи управління користувачами з трьома рівнями доступу. Система дозволяє реєструвати користувачів, управляти їх ролями, переглядати список користувачів та видаляти облікові записи (в залежності від прав доступу).

### Основні можливості:
- ✅ Реєстрація та аутентифікація користувачів
- ✅ JWT-токени для захисту API
- ✅ Три ролі: User, Admin, SuperAdmin
- ✅ Управління ролями користувачів
- ✅ Захищені маршрути на клієнті

---

## 🚀 Функціональні можливості

### Для всіх користувачів:
- Реєстрація нового облікового запису
- Вхід/вихід з системи
- Перегляд власного профілю

### Для Admin:
- Всі можливості User
- Перегляд списку всіх користувачів
- Видалення користувачів з роллю User
- Зміна ролі користувачів (User ↔ Admin)

### Для SuperAdmin:
- Всі можливості Admin
- Видалення будь-яких користувачів (крім інших SuperAdmin)
- Повний контроль над системою

---

## 🛠 Технології

### Frontend (Client)
- **React 19.2** - UI бібліотека
- **React Router DOM 7.9** - маршрутизація
- **Axios 1.13** - HTTP клієнт
- **Vite 6.0** - інструмент для збірки
- **CSS3** - стилізація

### Backend (Server)
- **Node.js** - JavaScript runtime
- **Express 5.1** - веб-фреймворк
- **JWT (jsonwebtoken 9.0)** - аутентифікація
- **bcryptjs 3.0** - хешування паролів
- **CORS** - підтримка кросдоменних запитів

---

## 📁 Структура проєкту

```
pz7_8/
├── client/                      # React frontend
│   ├── src/
│   │   ├── components/          # React компоненти
│   │   │   ├── Header.jsx       # Шапка сайту
│   │   │   ├── UserTable.jsx    # Таблиця користувачів
│   │   │   └── ProtectedRoute.jsx # Захист маршрутів
│   │   ├── pages/               # Сторінки
│   │   │   ├── LoginPage.jsx    # Сторінка входу
│   │   │   ├── RegisterPage.jsx # Сторінка реєстрації
│   │   │   ├── UserPage.jsx     # Кабінет користувача
│   │   │   ├── AdminPage.jsx    # Панель адміністратора
│   │   │   ├── SuperAdminPage.jsx # Панель суперадміна
│   │   │   └── UnauthorizedPage.jsx # Помилка доступу
│   │   ├── context/             # React Context
│   │   │   └── AuthContext.jsx  # Контекст аутентифікації
│   │   ├── services/            # API сервіси
│   │   │   ├── api.js          # Axios налаштування
│   │   │   ├── authService.js  # Аутентифікація
│   │   │   └── userService.js  # Робота з користувачами
│   │   ├── App.jsx             # Головний компонент
│   │   └── main.jsx            # Точка входу
│   └── package.json
│
├── server/                      # Express backend
│   ├── routes/                  # API маршрути
│   │   ├── auth.js             # Аутентифікація
│   │   └── users.js            # Користувачі
│   ├── middleware/              # Middleware
│   │   └── auth.js             # JWT перевірка
│   ├── data/                    # Дані (БД)
│   │   └── users.js            # Масив користувачів
│   ├── index.js                # Точка входу сервера
│   └── package.json
│
├── start.bat                    # Скрипт запуску (Windows)
└── README.md                    # Документація
```

---

## ⚙️ Встановлення та запуск

### Передумови
Переконайтеся, що у вас встановлено:
- **Node.js** (версія 16 або вище)

### Крок 1: Клонування репозиторію
```bash
git clone <url-репозиторію>
cd pz7_8
```

### Крок 2: Встановлення залежностей

**Backend:**
```bash
cd server
npm install
npm start
```
Сервер запуститься на http://localhost:5000

**Frontend:**
```bash
cd client
npm install
npm run dev
```
Клієнт запуститься на http://localhost:5173

### Крок 3: Відкрийте браузер
Перейдіть на http://localhost:5173

---

## 👥 Ролі користувачів

| Роль           | Права доступу                                                                                       |
|----------------|-----------------------------------------------------------------------------------------------------|
| **User**       | • Доступ до списку всіх користувачів                                                                |
| **Admin**      | • Всі права User<br>• Перегляд всіх користувачів<br>• Видалення User<br>• Зміна ролі (User ↔ Admin) |
| **SuperAdmin** | • Всі права Admin<br>• Видалення Admin<br>• Повний контроль                                         |

---

## 📡 API Endpoints

### Аутентифікація
```
POST /api/auth/register   # Реєстрація нового користувача
POST /api/auth/login      # Вхід в систему
```

### Користувачі (захищені роути)
```
GET    /api/users         # Отримати список користувачів (Admin, SuperAdmin)
DELETE /api/users/:id     # Видалити користувача (Admin, SuperAdmin)
PUT    /api/users/:id/role # Змінити роль користувача (Admin, SuperAdmin)
```

### Приклад запиту:
```http request
// Вхід в систему
POST http://localhost:5000/api/auth/login
Content-Type: application/json

{
  "email": "admin@example.com",
  "password": "admin123"
}
```

---

## 🔑 Тестові облікові дані

Для тестування використовуйте наступні облікові записи:

| Email                    | Пароль          | Роль       |
|--------------------------|-----------------|------------|
| `superadmin@example.com` | `superadmin123` | SuperAdmin |
| `admin@example.com`      | `admin123`      | Admin      |
| `user1@example.com`      | `user123`       | User       |
| `user2@example.com`      | `user123`       | User       |
